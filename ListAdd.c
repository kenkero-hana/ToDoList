#include <stdio.h>
#include <stdlib.h>

#include "task.h"
#include "init.h"
#include "add.h"

void addTask(){
    Task tasks[TASK_MAX];
    int i;
    //char text;

    printf("一日のやることを記載して\n");
    //for(i = 0; i < numTask; i++){
        if(numTask == (int)NULL){
            scanf("%s", tasks[i].text);
        }
        else{
            return;
        }
    //}

    
    // FILE *f;

    // f = fopen("text.txt", "W");
    // if(f == NULL){
    //     return;
    // }
    // fclose(f);
}